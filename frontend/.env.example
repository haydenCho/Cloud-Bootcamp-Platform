# Frontend 전용 환경 변수. 실제 값은 frontend/.env 에.
# Vite 는 VITE_ 접두사 변수만 클라이언트로 노출합니다.
# 로컬: http://localhost:8080 / 배포: http://lib.solcho.com (nginx 가 /api 를 백엔드로 프록시)
VITE_API_BASE_URL=http://localhost:8080

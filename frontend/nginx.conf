server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅: 정적 파일이 없으면 index.html 로 폴백
    location / {
        try_files $uri $uri/ /index.html;
    }
}
